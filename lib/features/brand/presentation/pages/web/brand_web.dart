import 'package:flutter/material.dart';

class BrandWeb extends StatelessWidget {
  const BrandWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Brands')),
      body: const Center(child: Text('Brand Web')),
    );
  }
}
